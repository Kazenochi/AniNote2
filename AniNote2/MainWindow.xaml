<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="AniNote2.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AniNote2"
    xmlns:view="using:AniNote2.MVM.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid  d:DataContext="{d:DesignInstance Type=local:MainWindow}" x:Name="RootPanel" Background="Green">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1000"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0">Hello</TextBlock>
        <ContentControl Content="{Binding animeListView}"/>
        <Grid Grid.Column="1" Background="Green">
            <Button Content="Helloooo" Command="{Binding ButtonPressedCommand}" VerticalAlignment="Bottom"/>
            
        </Grid>
        <Image Grid.Column="2" Source="/DevAssets/Bungo.jpg" />
    </Grid>
</Window>
